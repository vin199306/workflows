name: Build-OTA-Package-ARMv7

on:
  workflow_dispatch:
    inputs:
      version:
        description: '版本号（例如：1.0.0）'
        required: false
        type: string
        default: 'auto'
      use_upx:
        description: 'build with upx'
        required: false
        type: boolean
        default: true
      release_type:
        description: '发布类型（Release type）'
        required: true
        type: choice
        default: '预发布版（pre-release）'
        options:
          - '最新正式版（latest release）'
          - '预发布版（pre-release）'

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

permissions:
  contents: write

jobs:
  prepare:
    name: Resolve version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      current_version: ${{ steps.version.outputs.current_version }}
      version_source: ${{ steps.version.outputs.version_source }}
    steps:
      - uses: actions/checkout@v4
      - name: Read VERSION
        id: version
        env:
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          CURRENT_VERSION=$(tr -d '[:space:]' < VERSION)
          VERSION=$(printf '%s' "$INPUT_VERSION" | tr -d '[:space:]')
          if [ -z "$VERSION" ] || [ "$VERSION" = "auto" ]; then
            VERSION="$CURRENT_VERSION"
            SOURCE="VERSION file"
          else
            SOURCE="workflow input"
          fi
          if ! printf '%s' "$VERSION" | grep -Eq '^[0-9]+(\.[0-9]+){1,3}([-.][0-9A-Za-z]+)*$'; then
            echo "Invalid version: $VERSION" >&2
            exit 1
          fi
          {
            echo "version=$VERSION"
            echo "current_version=$CURRENT_VERSION"
            echo "version_source=$SOURCE"
          } >> "$GITHUB_OUTPUT"

  build:
    name: Build ${{ matrix.target }}
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        include:
          # 高通410 (MSM8916) = Cortex-A53，安卓4.4 仅运行在 32 位模式
          # 因此使用 armv7-unknown-linux-musleabihf，在 x86_64 runner 上交叉编译
          - target: armv7-unknown-linux-musleabihf
            runner: ubuntu-latest
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync version numbers
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          printf '%s\n' "$VERSION" > VERSION
          sed -i "s/^version = \"[^\"]*\"/version = \"$VERSION\"/" backend/Cargo.toml
          sed -i "s/\"version\": \"[^\"]*\"/\"version\": \"$VERSION\"/" frontend/package.json

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install native build tools
        run: |
          sudo apt-get update
          # 原工作流使用 apt musl-tools（仅能编译同架构），
          # armv7 交叉编译需使用 musl.cc 预编译的 armv7l-linux-musleabihf 工具链
          curl -fsSL https://musl.cc/armv7l-linux-musleabihf-cross.tgz -o /tmp/armv7l-musl-cross.tgz
          sudo tar -xzf /tmp/armv7l-musl-cross.tgz -C /opt/
          rm -f /tmp/armv7l-musl-cross.tgz
          echo "/opt/armv7l-linux-musleabihf-cross/bin" >> "$GITHUB_PATH"
          if [ "${{ inputs.use_upx }}" = "true" ]; then
            sudo apt-get install -y upx-ucl
          fi
          armv7l-linux-musleabihf-gcc --version

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            backend/target/
          key: ${{ runner.os }}-${{ runner.arch }}-${{ matrix.target }}-cargo-${{ hashFiles('backend/Cargo.lock') }}

      - name: Build backend
        working-directory: backend
        env:
          TARGET: ${{ matrix.target }}
          SQLITE3_STATIC: '1'
          LIBSQLITE3_SYS_USE_PKG_CONFIG: '0'
          # 高通410 (MSM8916) Cortex-A53：指定 target-cpu 自动启用 VFPv4 + NEON
          RUSTFLAGS: '-C target-cpu=cortex-a53'
        run: |
          case "$TARGET" in
            armv7-unknown-linux-musleabihf)
              export CC_armv7_unknown_linux_musleabihf=armv7l-linux-musleabihf-gcc
              export CARGO_TARGET_ARMV7_UNKNOWN_LINUX_MUSLEABIHF_LINKER=armv7l-linux-musleabihf-gcc
              ;;
          esac
          cargo build --locked --release --target "$TARGET"
          file "target/$TARGET/release/simadmin"

      - name: Compress backend with UPX
        if: inputs.use_upx == true
        env:
          TARGET: ${{ matrix.target }}
        run: upx --best --lzma "backend/target/$TARGET/release/simadmin"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Build frontend
        working-directory: frontend
        run: |
          pnpm install --frozen-lockfile
          pnpm run lint
          pnpm exec vite build

      - name: Package OTA
        env:
          TARGET: ${{ matrix.target }}
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          # pack-ota.sh 原生仅支持 aarch64 / x86_64，临时注入 armv7 target 支持
          python3 << 'PYEOF'
          with open('scripts/pack-ota.sh', 'r') as f:
              s = f.read()
          # 参数解析分支
          s = s.replace(
              '--target=x86_64|--target=amd64|--target=x86_64-unknown-linux-musl)',
              '--target=x86_64|--target=amd64|--target=x86_64-unknown-linux-musl)\n            --target=armv7|--target=armv7l|--target=armv7-unknown-linux-musleabihf)\n                TARGET="armv7-unknown-linux-musleabihf"\n                ;;'
          )
          # 归一化分支
          s = s.replace(
              'x86_64|amd64) TARGET="x86_64-unknown-linux-musl" ;;',
              'x86_64|amd64) TARGET="x86_64-unknown-linux-musl" ;;\n    armv7|armv7l) TARGET="armv7-unknown-linux-musleabihf" ;;\n    armv7-unknown-linux-musleabihf) ;;'
          )
          with open('scripts/pack-ota.sh', 'w') as f:
              f.write(s)
          PYEOF

          ./scripts/pack-ota.sh --target="$TARGET"
          case "$TARGET" in
            armv7-unknown-linux-musleabihf)
              ARCH_SUFFIX="armv7"
              ;;
          esac
          SOURCE="release/simadmin_${VERSION}_${TARGET}.tar.gz"
          DEST="release/simadmin-${ARCH_SUFFIX}.tar.gz"
          mv "$SOURCE" "$DEST"
          sha256sum release/*.tar.gz

      - name: Upload OTA artifact
        uses: actions/upload-artifact@v4
        with:
          name: ota-${{ matrix.target }}
          path: release/*.tar.gz
          if-no-files-found: error

  publish:
    name: Publish release
    needs: [prepare, build]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download OTA artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: ota-*
          path: release
          merge-multiple: true

      - name: Generate checksums and release notes
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
          RELEASE_TYPE: ${{ inputs.release_type }}
          COMMIT_SHA: ${{ github.sha }}
          REPOSITORY: ${{ github.repository }}
        run: |
          cd release
          sha256sum -- ./*.tar.gz > SHA256SUMS
          cd ..
          BUILD_TIME=$(TZ='Asia/Shanghai' date +'%Y/%m/%d %H:%M:%S')
          export BUILD_TIME
          python3 << 'EOF'
          import os, sys, re, subprocess
          version = os.environ.get('VERSION', '')
          commit_sha = os.environ.get('COMMIT_SHA', '')
          release_type = os.environ.get('RELEASE_TYPE', '预发布版（pre-release）')
          if not release_type:
              release_type = '预发布版（pre-release）'
          repository = os.environ.get('REPOSITORY', '3899/SimAdmin')
          build_time = os.environ.get('BUILD_TIME', '')
          changelog_path = 'docs/changelog.md'
          changelog_text = ''
          prev_tag = ''
          if os.path.exists(changelog_path):
              with open(changelog_path, 'r', encoding='utf-8') as f:
                  content = f.read()

              pattern = rf'### 📌 v{re.escape(version)}\s*\n(.*?)(?=\n### 📌 v|\Z)'
              match = re.search(pattern, content, re.DOTALL)
              if match:
                  changelog_text = match.group(1).strip()

              versions = re.findall(r'### 📌 v(\d+\.\d+\.\d+)', content)
              if version in versions:
                  idx = versions.index(version)
                  if idx + 1 < len(versions):
                      prev_tag = f'v{versions[idx + 1]}'
          if not prev_tag:
              try:
                  tags = subprocess.check_output(['git', 'tag', '--sort=-v:refname'], text=True).strip().splitlines()
                  tags = [t for t in tags if t.startswith('v') and t != f'v{version}']
                  if tags:
                      prev_tag = tags[0]
              except Exception:
                  pass
          if not prev_tag:
              prev_tag = 'v1.0.0'
          if not changelog_text:
              changelog_text = '- 详见 Commit 提交记录。'
          full_changelog_line = f'**Full Changelog**: https://github.com/{repository}/compare/{prev_tag}...v{version}'
          body = (
              f'## OTA 更新包\n\n'
              f'### 版本信息\n\n'
              f'- **版本**: {version}\n'
              f'- **Commit**: {commit_sha}\n'
              f'- **构建时间**: {build_time}\n'
              f'- **发布类型**: {release_type}\n'
              f'- **目标平台**: 安卓4.4 (KitKat) + 高通骁龙410 (MSM8916 / Cortex-A53)\n'
              f'- **编译目标**: `armv7-unknown-linux-musleabihf` (musl 静态链接)\n'
              f'- **资产包**: \n'
              f'\t- **armv7 (MSM8916 / Android 4.4)**: `simadmin-armv7.tar.gz`\n\n'
              f'### 更新说明\n\n'
              f'{changelog_text}\n\n\n'
              f'{full_changelog_line}\n'
          )
          with open('release/RELEASE_BODY.md', 'w', encoding='utf-8') as f:
              f.write(body)
          EOF

      - name: Publish OTA packages
        uses: softprops/action-gh-release@v2
        with:
          # 使用独立 tag 后缀，避免与主构建 (aarch64/x86_64) 的 release 冲突
          tag_name: v${{ needs.prepare.outputs.version }}-armv7
          name: SimAdmin_v${{ needs.prepare.outputs.version }}_armv7-MSM8916
          files: |
            release/simadmin-armv7.tar.gz
          body_path: release/RELEASE_BODY.md
          overwrite_files: true
          generate_release_notes: false
          draft: false
          prerelease: ${{ inputs.release_type == '预发布版（pre-release）' }}
          make_latest: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build summary
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
          RELEASE_TYPE: ${{ inputs.release_type }}
          COMMIT_SHA: ${{ github.sha }}
          REPOSITORY: ${{ github.repository }}
        run: |
          BUILD_TIME=$(TZ='Asia/Shanghai' date +'%Y/%m/%d %H:%M:%S')
          export BUILD_TIME
          python3 << 'EOF'
          import os
          version = os.environ.get('VERSION', '')
          commit_sha = os.environ.get('COMMIT_SHA', '')
          release_type = os.environ.get('RELEASE_TYPE', '预发布版（pre-release）')
          if not release_type:
              release_type = '预发布版（pre-release）'
          repository = os.environ.get('REPOSITORY', '3899/SimAdmin')
          build_time = os.environ.get('BUILD_TIME', '')
          sums = {}
          sums_file = 'release/SHA256SUMS'
          if os.path.exists(sums_file):
              with open(sums_file, 'r', encoding='utf-8') as f:
                  for line in f:
                      parts = line.strip().split()
                      if len(parts) >= 2:
                          h, fn = parts[0], os.path.basename(parts[1])
                          sums[fn] = h
          armv7_hash = sums.get('simadmin-armv7.tar.gz', 'N/A')
          release_url = f'https://github.com/{repository}/releases/tag/v{version}-armv7'
          summary = (
              f'## 🚀 SimAdmin OTA 构建完成 (armv7 / MSM8916)\n\n'
              f'### 📌 版本与构建元数据\n\n'
              f'- **版本**: `{version}`\n'
              f'- **Commit**: `{commit_sha}`\n'
              f'- **构建时间**: `{build_time}`\n'
              f'- **发布类型**: {release_type}\n'
              f'- **目标平台**: 安卓4.4 + 高通410 (MSM8916)\n\n'
              f'### 📦 资产包与 SHA-256 校验和\n\n'
              f'| 架构 / 平台 | 文件名 | SHA-256 校验和 |\n'
              f'| :--- | :--- | :--- |\n'
              f'| **armv7 / MSM8916** | `simadmin-armv7.tar.gz` | `{armv7_hash}` |\n\n'
              f'---\n'
              f'🎉 Release 发布完成: [{repository}_v{version}_armv7]({release_url})\n'
          )
          step_summary = os.environ.get('GITHUB_STEP_SUMMARY')
          if step_summary:
              with open(step_summary, 'a', encoding='utf-8') as f:
                  f.write(summary)
          EOF
